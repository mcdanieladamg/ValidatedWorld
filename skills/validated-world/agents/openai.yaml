interface:
  display_name: "ValidatedWorld"
  short_description: "Use local semantic change control"
  default_prompt: "Use ValidatedWorld's Python engine in English to inspect and safely update this project's local knowledge graph."
